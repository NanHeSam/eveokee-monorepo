/**
 * Layout constants for consistent spacing and sizing
 * These are design constants, not device-specific measurements
 */

// React Navigation bottom tab bar base height (without safe area)
// This is a standard height used by React Navigation's bottom tab navigator
export const TAB_BAR_BASE_HEIGHT = 60;

// Standard spacing values
export const BUTTON_SPACING = 16;
export const EXTRA_PADDING = 24;

// Default fallback heights (used before dynamic measurement completes)
export const DEFAULT_BUTTON_HEIGHT = 56;

