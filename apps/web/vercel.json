{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter web build:prerender",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
