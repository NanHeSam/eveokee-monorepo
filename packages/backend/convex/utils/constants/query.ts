/**
 * Query and pagination limits
 * Default limits for database queries to prevent OOM
 */

export const DEFAULT_CALL_JOBS_LIMIT = 50;
export const DEFAULT_CALL_JOBS_STATS_LIMIT = 500;
export const DEFAULT_CALL_SESSIONS_LIMIT = 50;
export const DEFAULT_DASHBOARD_STATS_LIMIT = 1000;

