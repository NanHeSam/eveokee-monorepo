{
  "name": "@backend/convex",
  "version": "1.0.0",
  "private": true,
  "main": "./convex/_generated/api.js",
  "types": "./convex/_generated/api.d.ts",
  "scripts": {
    "dev": "npx convex dev",
    "deploy": "npx convex deploy",
    "type-check": "echo 'Skipping type-check for backend (requires Convex runtime types)'",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed:blog": "npx tsx scripts/seed-blog.ts",
    "seed:blog:clean": "npx tsx scripts/seed-blog.ts --delete-existing"
  },
  "dependencies": {
    "@clerk/backend": "^2.17.2",
    "@sindresorhus/slugify": "^3.0.0",
    "@vapi-ai/server-sdk": "^0.10.2",
    "convex": "^1.28.0",
    "date-fns": "^3.6.0",
    "date-fns-tz": "^3.2.0",
    "openai": "^4.79.0",
    "svix": "^1.76.1"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@vitest/spy": "^3.2.4",
    "convex-test": "^0.0.38",
    "vitest": "^2.1.4"
  }
}
